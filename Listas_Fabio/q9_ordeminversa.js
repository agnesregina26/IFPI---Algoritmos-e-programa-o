
import * as readsync from 'readline-sync'

//Entrada
const a = Number(readsync.question('Numero A = '))
const b = Number(readsync.question ('Numero B = '))

//Saída
console.log('A ordem inversa dos numeros é: ', 'Numero b: ', b, 'Numero A= ', a)